import 'package:flutter/material.dart';

/// Application color constants
class AppColors {
  AppColors._();

  static const primaryColor = Color(0xFF4F46E5);
  static const secondaryColor = Color(0xFF7C3AED);
  static const accentColor = Color(0xFF06B6D4);

  // Light theme colors
  static const lightSurfaceColor = Color(0xFFF6F7FB);
  static const lightCardColor = Colors.white;
  static const lightTextColor = Color(0xFF1F2937);

  // Dark theme colors
  static const darkSurfaceColor = Color(0xFF111827);
  static const darkCardColor = Color(0xFF1F2937);
  static const darkTextColor = Color(0xFFF9FAFB);

  // Transport colors
  static const trenintColor = Color(0xFF1565C0);
  static const trenitaliSecondaryColor = Color(0xFF42A5F5);
  static const cotralColor = Color(0xFF059669);
  static const cotralSecondaryColor = Color(0xFF34D399);
  static const magniColor = Color(0xFFD97706);
  static const magniSecondaryColor = Color(0xFFFBBF24);

  // Navigation colors
  static const navigationPrimary = Color(0xFF4F46E5);
  static const navigationSecondary = Color(0xFF7C3AED);

  // QR Scanner colors
  static const qrPrimary = Color(0xFF059669);
  static const qrSecondary = Color(0xFF34D399);

  // Weather gradient colors
  static const weatherPrimary = Color(0xFF7C3AED);
  static const weatherSecondary = Color(0xFF06B6D4);

  // Status colors
  static const successColor = Color(0xFF10B981);
  static const warningColor = Color(0xFFF59E0B);
  static const errorColor = Colors.red;
  static const infoColor = Color(0xFF3B82F6);
}
